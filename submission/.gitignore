# LaTeX build artifacts — regenerated by compile.sh / `make` (and removed by `make clean`).
# Includes the multibib aux/bbl/blg for the Meth and Supp bibliographies.
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.bcf
*.run.xml
*.nav
*.snm
*.vrb

# Build output PDF (regenerable). Figure PDFs under fig/ are committed and NOT ignored.
/paper.pdf
